name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-go@v6.4.0
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...

  build:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goarch: amd64
            out_suffix: amd64
          - goarch: arm64
            out_suffix: arm64
          - goarch: "386"
            out_suffix: i386
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-go@v6.4.0
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum
      - name: Version from tag
        id: meta
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
      - name: Build
        env:
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          VERSION="${{ steps.meta.outputs.version }}"
          RELEASE_DATE=$(date -u +%Y-%m-%d)
          VP="github.com/ghostpsy/agent-linux/internal/version"
          mkdir -p dist
          OUT="dist/ghostpsy_${VERSION}_linux_${{ matrix.out_suffix }}"
          go build -trimpath -ldflags="-s -w -X ${VP}.Version=${VERSION} -X ${VP}.ReleaseDate=${RELEASE_DATE}" -o "$OUT" ./cmd/ghostpsy
      - uses: actions/upload-artifact@v7.0.0
        with:
          name: ghostpsy-linux-${{ matrix.out_suffix }}
          path: dist/ghostpsy_*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-go@v6.4.0
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum
      - uses: actions/download-artifact@v6.0.0
        with:
          pattern: ghostpsy-linux-*
          path: release-assets
          merge-multiple: true
      - name: SHA256SUMS
        run: |
          cd release-assets && sha256sum ghostpsy_* | tee SHA256SUMS
      - name: Sign SHA256SUMS (Ed25519)
        env:
          GHOSTPSY_RELEASE_SIGNING_KEY_HEX: ${{ secrets.GHOSTPSY_RELEASE_SIGNING_KEY_HEX }}
        run: |
          if [ -z "${GHOSTPSY_RELEASE_SIGNING_KEY_HEX}" ]; then
            echo "GHOSTPSY_RELEASE_SIGNING_KEY_HEX secret is not set; refusing to publish an unsigned release." >&2
            exit 1
          fi
          go run ./scripts/sign-release release-assets/SHA256SUMS
          test -f release-assets/SHA256SUMS.sig
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2.2.2
        with:
          files: |
            release-assets/ghostpsy_*
            release-assets/SHA256SUMS
            release-assets/SHA256SUMS.sig
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
